# 코어 라이브러리
requests>=2.31.0
numpy>=1.24.0
pandas>=2.0.0

# 웹 프레임워크
streamlit>=1.28.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
jinja2>=3.1.2
python-multipart>=0.0.6

# LLM
openai>=1.3.0
anthropic>=0.7.0

# RAG & 임베딩
sentence-transformers>=2.2.0

# 유틸리티
python-dotenv>=1.0.0
openpyxl>=3.1.0


black>=23.0.0

# RAG & Embeddings
# Keep this unpinned here because a newer lower section requires >=2.7.0.
# Pinning to 2.3.1 makes resolution impossible.
faiss-cpu==1.7.4  # Optional: for faster search

# Web Interface
streamlit==1.31.0

# Data Processing
pandas==2.0.3

# Logging
loguru==0.7.2

# Testing (Optional)
pytest==8.0.0
pytest-asyncio==0.23.0

# ── Core pipeline ──────────────────────────────────────────────────────────
langgraph>=0.1.0
langchain>=0.2.0
langchain-community>=0.2.0

# ── LLM ────────────────────────────────────────────────────────────────────
openai>=1.0.0
google-genai>=0.3.0

# ── Retrieval ───────────────────────────────────────────────────────────────
faiss-cpu>=1.7.4            # swap for faiss-gpu on GPU machines
sentence-transformers>=2.7.0  # embedding + cross-encoder reranker

# ── Date parsing ────────────────────────────────────────────────────────────
python-dateutil>=2.8.2      # FIX: was missing; deadline_tool uses dateutil.parser
                            # Falls back to stdlib if absent, but dateutil handles
                            # more Korean/ISO date formats robustly.

# ── Utilities ───────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
numpy>=1.24.0
